buildscript {
<<<<<<< HEAD
    repositories {
        google() // ✅ Required for Firebase
        mavenCentral()
    }
    dependencies {
        classpath 'com.android.tools.build:gradle:8.1.4' // ✅ Make sure this is present
        classpath 'com.google.gms:google-services:4.4.2' // ✅ Required for Firebase
    }
}

=======
    ext.kotlin_version = "1.7.10"

    repositories {
        google()  // ✅ Make sure this exists
        mavenCentral()  // ✅ Make sure this exists
    }

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
        classpath "com.android.tools.build:gradle:8.1.0"  // Ensure this is added
        classpath "com.google.gms:google-services:4.4.0"  // Ensure this is added for Firebase
    }
}
>>>>>>> origin/master
allprojects {
    repositories {
        google()
        mavenCentral()
    }
}

rootProject.buildDir = "../build"
subprojects {
    project.buildDir = "${rootProject.buildDir}/${project.name}"
}
subprojects {
    project.evaluationDependsOn(":app")
}

tasks.register("clean", Delete) {
    delete rootProject.buildDir
}
buildscript {
    ext.kotlin_version = "1.7.10"  // Make sure this version is correct

    dependencies {
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:$kotlin_version"
    }
}
